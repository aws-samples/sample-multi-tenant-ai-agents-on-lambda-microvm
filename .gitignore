# Python / Lambda packaging artifacts (regenerated by deploy.sh)
.venv/
pkg/
__pycache__/
*.pyc

# Local build scratch
*.zip
/tmp/

# OS / editor
.DS_Store
*.swp
.idea/
.vscode/

# never commit real secrets or per-account state
*.env
secrets.*
src/.gateway-token

# local Claude Code / skills-CLI artifacts (session tooling, not part of the project)
.agents/
.claude/
skills-lock.json

# kept locally but excluded from the repo (intermediate iterations + raw verification logs)
prototypes/
docs/results/
